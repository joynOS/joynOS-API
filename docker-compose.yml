version: '3.9'
services:
  db:
    image: postgres:16-alpine
    container_name: joynos_db
    environment:
      POSTGRES_USER: joyn
      POSTGRES_PASSWORD: joyn
      POSTGRES_DB: joyn
    ports:
      - '5434:5432'
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
